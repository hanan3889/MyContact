<controls:MetroWindow x:Class="MyContact.View.AddEditServiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Ajouter / Modifier un Service"
        Height="450" Width="600">

    <Grid Margin="10">
        <StackPanel>
            <!-- Champ Nom du Service -->
            <TextBlock Text="Nom du Service :" FontSize="16" FontWeight="Bold"/>
            <TextBox Text="{Binding SelectedService.Nom, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,5,0,10" Height="30"/>

            <!-- Boutons Annuler et Enregistrer -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Annuler" Width="100" Command="{Binding CancelCommand}"/>
                <Button Content="Enregistrer" Width="120" Command="{Binding SaveCommand}"/>
            </StackPanel>

            <!-- Tableau des Services -->
            <DataGrid x:Name="ServicesDataGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                      ItemsSource="{Binding Services, UpdateSourceTrigger=PropertyChanged}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Nom du Service" Binding="{Binding Nom}" Width="*"/>
                    <DataGridTemplateColumn Header="Actions">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Modifier" Margin="2"
                                            Command="{Binding DataContext.EditServiceCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="Supprimer" Margin="2"
                                            Command="{Binding DataContext.DeleteServiceCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>
    </Grid>
</controls:MetroWindow>
