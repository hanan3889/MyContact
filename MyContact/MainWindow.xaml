<controls:MetroWindow x:Class="MyContact.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:local="clr-namespace:MyContact"
                      xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:viewmodels="clr-namespace:MyContact.ViewModels"
                      d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
                      mc:Ignorable="d"
                      Title="Accueil" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#339c33"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#339c33"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <!-- Boutons de recherche à gauche -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Rechercher un salarié"
                        Width="150"
                        Height="30"
                        Margin="10"
                        Command="{Binding OpenSearchSalaryViewCommand}"
                        Style="{StaticResource RoundedButtonStyle}" />

                <Button Content="Par service"
                        Width="150"
                        Height="30"
                        Margin="10"
                        Padding="0"
                        Command="{Binding OpenSearchSalaryByServiceViewCommand}"
                        Style="{StaticResource RoundedButtonStyle}" />

                <Button Content="Par site"
                        Width="150"
                        Height="30"
                        Margin="10"
                        Padding="0"
                        Command="{Binding OpenSearchSalaryByCityViewCommand}"
                        Style="{StaticResource RoundedButtonStyle}" />
            </StackPanel>

            <!-- Boutons Connexion et Enregistrement à droite -->
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Connexion Admin"
                        Width="150"
                        Height="30"
                        Margin="10"
                        Padding="0"
                        Command="{Binding OpenLoginCommand}"
                        Style="{StaticResource RoundedButtonStyle}"/>

                <Button Content="S'enregistrer"
                        Width="150"
                        Height="30"
                        Margin="10"
                        Padding="0"
                        Command="{Binding OpenRegisterCommand}"
                        Style="{StaticResource RoundedButtonStyle}"/>
            </StackPanel>
        </DockPanel>

        <DataGrid x:Name="ContactsDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  Margin="10"
                  ItemsSource="{Binding Salaries}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Nom}" />
                <DataGridTextColumn Header="Prénom" Binding="{Binding Prenom}" />
                <DataGridTextColumn Header="Téléphone portable" Binding="{Binding TelephonePortable}" />
                <DataGridTextColumn Header="Ligne fixe" Binding="{Binding TelephoneFixe}" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" />
                <DataGridTextColumn Header="Ville" Binding="{Binding SiteVille}" />
                <DataGridTextColumn Header="Service" Binding="{Binding ServiceNom}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</controls:MetroWindow>
